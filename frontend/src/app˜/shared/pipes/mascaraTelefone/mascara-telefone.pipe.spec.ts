import { MascaraTelefonePipe } from './mascara-telefone.pipe';

describe('MascaraTelefonePipe', () => {
  it('create an instance', () => {
    const pipe = new MascaraTelefonePipe();
    expect(pipe).toBeTruthy();
  });
});
